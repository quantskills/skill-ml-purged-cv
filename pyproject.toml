[build-system]
requires = ["setuptools>=77"]
build-backend = "setuptools.build_meta"

[project]
name = "purged-kfold-validation"
version = "0.9.0"
description = "Leakage-safe financial validation evidence with Purged K-Fold, deterministic CPCV paths, causal Walk-Forward, and PIT provenance."
readme = "README.md"
license = "MIT"
license-files = ["LICENSE"]
requires-python = ">=3.11"
dependencies = ["numpy>=1.26"]

[project.scripts]
purged-cv-upload = "purged_kfold_validation.cli:main"
purged-cv-skill = "purged_kfold_validation.agent_cli:main"
purged-cv-strategy = "purged_kfold_validation.strategy_cli:main"
purged-cv-forward = "purged_kfold_validation.forward_cli:main"

[project.optional-dependencies]
dev = [
  "build>=1.2",
  "hypothesis>=6.100",
  "mypy>=1.15",
  "packaging>=24,<26",
  "pandas-stubs>=2.2",
  "pytest>=8",
  "ruff==0.15.21",
  "twine>=6,<7",
]
pandas = ["pandas>=2.1"]
benchmark = ["pandas>=2.1", "pyarrow>=15"]
upload = ["pandas>=2.1", "pyarrow>=15"]
temporal-models = ["lightgbm>=4.0", "scikit-learn>=1.4", "torch>=2.2"]

[tool.setuptools.package-data]
purged_kfold_validation = [
  "resources/agent_skill/*.json",
  "resources/feature_upload/*.json",
  "resources/feature_upload/examples/*/*.csv",
  "resources/feature_upload/examples/*/*.json",
  "resources/strategy_benchmark/*.json",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]

[tool.mypy]
files = ["src", "tests", "tasks"]
strict = true
show_error_codes = true
pretty = true

[[tool.mypy.overrides]]
module = ["pyarrow", "pyarrow.*"]
ignore_missing_imports = true
